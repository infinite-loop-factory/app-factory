# Maestro E2E Test Configuration
#
# Prerequisites for `requires-auth` flows:
#   1. App must be built with EXPO_PUBLIC_E2E_MODE=true (dev/preview only)
#   2. Run setup/00-seed-auth.yaml once to inject Supabase session
#   3. Subsequent flows use clearState: false to reuse the session
#
# See widget-manual-verification.md § "E2E 인증 설정" for full guide.
#
# Run:
#   maestro test maestro/setup/00-seed-auth.yaml
#   maestro test maestro/flows/

appId: com.gracefullight.countrytracker
name: Country Tracker E2E Tests
