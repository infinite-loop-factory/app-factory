appId: com.gracefullight.countrytracker
name: Share Stats Reachable Smoke
tags:
  - requires-auth
  - requires-data
  - ui-primitives
  - draft
---
# Map share button selector + map screen content는 별도 PR. 본 flow는 tab 노출만.

- runFlow: ../setup/boot.yaml
- assertVisible: "My Travels"
- assertVisible:
    id: "tab-map"
