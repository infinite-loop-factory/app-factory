// Shared styling constants for map components (web + native)

// Opacity for visited country fill overlay (0..1)
export const VISITED_FILL_OPACITY = 0.2;

// Stroke width for overlay outlines (in px)
export const VISITED_STROKE_WIDTH_WEB = 1;
export const VISITED_STROKE_WIDTH_NATIVE = 1;
